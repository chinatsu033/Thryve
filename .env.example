# Supabase (public anon key only — never commit service role)
VITE_SUPABASE_URL=https://srhoswkgjxqmasmaqjfg.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Cloudflare Turnstile (register-only human verification)
VITE_TURNSTILE_SITE_KEY=

# Pages Function secrets (Cloudflare Pages → Settings → Environment variables):
# TURNSTILE_SECRET_KEY=
# SUPABASE_SERVICE_ROLE_KEY=
# SUPABASE_URL= (if not reusing VITE_SUPABASE_URL in Functions)
# SECURITY_ANSWERS_PEPPER=  (openssl rand -hex 32; Pages secret)
